import { ApiConnectionSettings } from './api-connection-settings';

describe('ApiConnectionSettings', () => {
  it('should create an instance', () => {
    expect(new ApiConnectionSettings()).toBeTruthy();
  });
});
